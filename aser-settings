#!/usr/bin/env bash
# 🧠 aserdev's Rofi Control Center — Simplified with Screenrec Integration
# Minimal deps: rofi, notify-send, wl-clipboard, a terminal (kitty preferred)
set -euo pipefail
IFS=$'\n\t'

TERMINAL="${TERMINAL:-kitty}"
EDITOR="${EDITOR:-nano}"
ROFI_CMD="${ROFI_CMD:-rofi -dmenu -i -p}"

rofi_yn() {
  local prompt="${1:-Proceed?}"
  local ans
  ans=$(printf 'Yes\nNo' | rofi -dmenu -p "$prompt")
  [[ "$ans" == "Yes" ]]
}

ask_install() {
  local pkg="$1"
  local bin="${2:-$1}"
  if command -v "$bin" &>/dev/null; then
    return 0
  fi

  if ! rofi_yn "$pkg not found. Install?"; then
    return 1
  fi

  if command -v yay &>/dev/null; then
    $TERMINAL -e bash -c "yay -S --noconfirm $pkg || read -p 'Install failed, press enter...'"
  else
    $TERMINAL -e bash -c "sudo pacman -S --noconfirm $pkg || read -p 'Install failed, press enter...'"
  fi
}

open_in_thunar() {
  local path="${1:-$HOME}"
  if ! command -v thunar &>/dev/null; then
    ask_install thunar || xdg-open "$path" >/dev/null 2>&1 || $TERMINAL -e "$EDITOR" "$path"
  else
    thunar "$path" &
  fi
}

open_file_gui_or_terminal() {
  local file="$1"
  if command -v pluma &>/dev/null; then
    pluma "$file" &
  else
    $TERMINAL -e bash -c "$EDITOR '$file'; read -p 'Press enter to exit...'"
  fi
}

settings_menu() {
  local options=(
    "🌀  Hyprland"
    "🧭  Waybar"
    "🎛️  Rofi"
    "🖼️  Wallpaper"
    "⌨️  Keybinds"
    "🎨  Color Picker"
    "🔊  Audio Settings"
    "🗂️  All Settings"
    "⬅️  Back"
  )
  local choice=$(printf '%s\n' "${options[@]}" | rofi -dmenu -i -p "Settings")

  case "$choice" in
    "🌀  Hyprland") open_in_thunar "$HOME/.config/hypr" ;;
    "🧭  Waybar") open_in_thunar "$HOME/.config/waybar" ;;
    "🎛️  Rofi") open_in_thunar "$HOME/.config/rofi" ;;
    "🖼️  Wallpaper") if command -v waypaper &>/dev/null; then waypaper --open & else ask_install waypaper || rofi -e "No wallpaper tool found"; fi ;;
    "⌨️  Keybinds") [[ -f ~/.config/hypr/binds.conf ]] && open_file_gui_or_terminal "$HOME/.config/hypr/binds.conf" || rofi -e "binds.conf not found 💀" ;;
    "🎨  Color Picker") ask_install hyprpicker || return; color=$(hyprpicker -a 2>/dev/null || true); [[ -n "$color" ]] && wl-copy "$color" && notify-send "Color" "Copied: $color" || rofi -e "No color picked 💀" ;;
    "🔊  Audio Settings") ask_install pavucontrol && pavucontrol & ;;
    "🗂️  All Settings") open_in_thunar "$HOME/.config" ;;
    "⬅️  Back") main_menu ;;
  esac
}

install_menu() {
  local options=(
    "🏗️  AUR (yay)"
    "📦  Pacman"
    "📦  Flatpak"
    "🔄  Update All"
    "⬅️  Back"
  )
  local choice=$(printf '%s\n' "${options[@]}" | rofi -dmenu -i -p "Install")

  case "$choice" in
    "🏗️  AUR (yay)") pkg=$(rofi -dmenu -p "AUR package:"); [[ -n "$pkg" ]] && $TERMINAL -e "yay -S $pkg" ;;
    "📦  Pacman") pkg=$(rofi -dmenu -p "Pacman package:"); [[ -n "$pkg" ]] && $TERMINAL -e "sudo pacman -S $pkg" ;;
    "📦  Flatpak") pkg=$(rofi -dmenu -p "Flatpak ID:"); [[ -n "$pkg" ]] && $TERMINAL -e "flatpak install -y $pkg" ;;
    "🔄  Update All") $TERMINAL -e bash -c 'sudo pacman -Syu --noconfirm; flatpak update -y; yay -Syu --noconfirm; read -p "✅ Done! Press enter..."' ;;
    "⬅️  Back") main_menu ;;
  esac
}

clipboard_menu() {
  local options=("📋  Open Clipboard" "🧹  Clear Clipboard" "⬅️  Back")
  local choice=$(printf '%s\n' "${options[@]}" | rofi -dmenu -i -p "Clipboard")
  case "$choice" in
    "📋  Open Clipboard") [[ -x ~/.local/bin/rofi-cliphist ]] && bash ~/.local/bin/rofi-cliphist || ask_install cliphist ;;
    "🧹  Clear Clipboard") command -v cliphist &>/dev/null && cliphist wipe && notify-send "Clipboard" "Cleared" || rofi -e "No cliphist" ;;
    "⬅️  Back") main_menu ;;
  esac
}

sysinfo_menu() {
  local options=("🧠  Brokefetch" "⚡  Fastfetch" "📊  Btop" "⬅️  Back")
  local choice=$(printf '%s\n' "${options[@]}" | rofi -dmenu -i -p "System Info")
  case "$choice" in
    "🧠  Brokefetch") command -v brokefetch &>/dev/null && $TERMINAL -e brokefetch || ask_install brokefetch ;;
    "⚡  Fastfetch") ask_install fastfetch && $TERMINAL -e fastfetch ;;
    "📊  Btop") ask_install btop && $TERMINAL -e btop ;;
    "⬅️  Back") main_menu ;;
  esac
}

main_menu() {
  local options=(
    "⚙️  Settings"
    "🚀  Run Command"
    "📦  Install"
    "📋  Clipboard"
    "😀  Emoji Picker"
    "📸  Screen Recording"
    "🧠  System Info"
    "🔋  Power"
    "ℹ️  About"
  )
  local choice=$(printf '%s\n' "${options[@]}" | rofi -dmenu -i -p "Aser Control Center")
  case "$choice" in
    "⚙️  Settings") settings_menu ;;
    "🚀  Run Command") rofi -show run ;;
    "📦  Install") install_menu ;;
    "📋  Clipboard") clipboard_menu ;;
    "😀  Emoji Picker") rofi -show emoji ;;
    "📸  Screen Recording") ~/.local/bin/screenrec & ;;
    "🧠  System Info") sysinfo_menu ;;
    "🔋  Power") command -v wlogout &>/dev/null && wlogout || rofi -e "wlogout not installed" ;;
    "ℹ️  About") xdg-open "https://github.com/aserdevyt/aserdev-os/" ;;
    *) exit 0 ;;
  esac
}

main_menu
